const departments = {
    id: id,
    name: name
}

const roles = {
    id: id,
    title: title,
    salary: salary,
    department_id: department_id
}

const employees = {
    id: id,
    first_name: first_name,
    last_name: last_name,
    role_id: role_id,
    manager_id: manager_id
}